import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "next-themes";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SecureKeep - E2E Encrypted Notes",
  description: "A Google Keep alternative with end-to-end encryption. Your notes stay private - only you can read them.",
  keywords: ["secure notes", "E2E encryption", "privacy", "encrypted notes", "Google Keep alternative"],
  authors: [{ name: "SecureKeep" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "SecureKeep - E2E Encrypted Notes",
    description: "Your notes stay private - only you can read them.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
